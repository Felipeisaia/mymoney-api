generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url = "file:./dev.db"
}

model Transaction {
  id String @id @default(uuid())
  title String
  amount Float
  date DateTime @default(now()) 
  category String
  type String
}